# ProtCross Desktop Backend

Local sidecar backend for ProtCross Desktop 0.2.0. It is packaged with the
desktop application and pins the core `protcross` Python package used by the
GUI prediction workflow.
